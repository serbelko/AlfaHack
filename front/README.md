# 📱 Alfa Assistant Frontend  
**Интерфейс финансового помощника, разработанный под UX телефона iPhone 14 Pro.**  
Полностью адаптирован под мобильную логику: закруглённые зоны, навигация снизу, актуальные размеры элементов и визуальные паттерны iOS-UI.

---

## 🚀 Основное описание проекта

Этот фронтенд — клиентская часть приложения **Alfa Assistant**, которое помогает владельцам малого бизнеса управлять финансами, анализировать доходы, расходы, транзакции, а также получать рекомендации по улучшению финансовых показателей.

Проект написан на **React + Vite**, полностью оптимизирован под быструю работу и мобильный интерфейс.  
Обмен данными происходит через FastAPI-backend (JWT-авторизация + работа со счетами и историей операций).

---

# ✨ Особенности

- 📱 **UX под iPhone 14 Pro**  
  Все размеры, отступы, закругления, bottom-nav и header соответствуют реальному поведению iOS-интерфейсов.

- 🔐 **JWT авторизация**  
  Токен сохраняется в `localStorage` и используется для запросов к защищённым эндпоинтам.

- 📊 **Аналитика в реальном времени**  
  Графики строятся на основе live-данных: `/api/amount/history`.

- 💸 **Поддержка нескольких счетов**  
  Список счетов, суммы, транзакции, отслеживание изменений.

- 🤖 **Система рекомендаций**  
  Есть карточка с рекомендацией и отдельная страница Recommendation Details.

- 🎨 **Кастомные UI-компоненты**  
  — кнопки в стиле iOS  
  — smooth-анимации  
  — качественные тени  
  — кастомный Bottom Navigation Bar

---

## 📂 Структура проекта

src/
├── api/ # Работа с backend API
├── assets/ # Иконки, изображения, аватар ассистента
├── components/ # UI-компоненты
├── layouts/ # Основной layout с нижней навигацией
├── pages/ # Страницы приложения
│ ├── HomePage.jsx
│ ├── AnalyticsPage.jsx
│ ├── PromotionPage.jsx
│ ├── RecommendationDetailPage.jsx
│ ├── AccountsPage.jsx
│ └── ...
├── App.jsx # Маршрутизация
├── main.jsx # Точка входа
└── styles/ # Общие стили



---

# 🔧 Технологии

- React 18  
- Vite  
- React Router v6  
- Chart.js  
- JWT Auth  
- Кастомный HTTP-клиент

---

# 🛠 Установка и запуск

### 1. Установить зависимости
```bash
npm install

2. Создать файл .env
VITE_API_BASE_URL=http://localhost:8000


3. Запуск dev-сервера
npm run dev


🔌 Работа с backend

Проект взаимодействует с FastAPI-бэкендом, поддерживающим JWT авторизацию.

Основные запросы:

POST /api/auth/login — вход

GET /api/auth — проверка токена

GET /api/amount/ — список счетов

GET /api/amount?name=... — один счёт

GET /api/amount/history — история транзакций

POST /api/amount/transaction — добавить транзакцию

Все запросы отправляются с заголовком:

Authorization: Bearer <token>

| Route                       | Назначение               |
| --------------------------- | ------------------------ |
| `/login`                    | Авторизация              |
| `/`                         | Главная страница         |
| `/analytics`                | Аналитика                |
| `/analytics/recommendation` | Детализация рекомендации |
| `/promotion`                | Продвижение              |
| `/promotion/niche`          | Описание ниши            |
| `/promotion/competitor/:id` | Информация о конкуренте  |
| `/accounts`                 | Все счета                |
| `/payments`                 | Платежи                  |
| `/services`                 | Сервисы                  |
| `/contact`                  | Контакты                 |
| `/profile`                  | Профиль                  |



