# AlfaHack - Финансовый помощник для малого бизнеса

> Проект разработан для хакатона от Альфа-Банка

## 📋 О проекте

**AlfaHack** — это комплексное веб-приложение для управления финансами малого бизнеса, которое помогает предпринимателям анализировать свои финансовые потоки, изучать конкурентов и получать AI-рекомендации для развития бизнеса.

### Основные возможности:

- 💰 **Управление финансами** — отслеживание доходов и расходов, аналитика по категориям
- 🤖 **AI-анализ** — интеллектуальная классификация запросов и анализ конкурентов
- 📊 **Аналитика** — визуализация финансовых данных и трендов
- 🔍 **Исследование рынка** — автоматический поиск и анализ конкурентов
- 💡 **Рекомендации** — персонализированные советы на основе AI-анализа

---

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Git

### Запуск проекта

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd AlfaHack
   ```

2. **Создайте файл `.env`** в корне проекта (см. раздел "Конфигурация")

3. **Запустите все сервисы:**
   ```bash
   docker-compose up --build
   ```

4. **Откройте в браузере:**
   - Frontend: http://localhost
   - Backend API: http://localhost:8000
   - AI Service: http://localhost:8001
   - API документация: http://localhost:8000/docs

### Остановка сервисов

```bash
docker-compose down
```

Для полного удаления данных (включая volumes):
```bash
docker-compose down -v
```

---

## 🏗️ Архитектура системы

Проект построен на **микросервисной архитектуре** с четким разделением ответственности:

```
┌─────────────┐
│   Frontend  │ (React + Vite)
│   :80       │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────┐
│         Nginx (Reverse Proxy)       │
└──────┬──────────────────┬───────────┘
       │                  │
       ▼                  ▼
┌─────────────┐    ┌──────────────┐
│   Backend   │    │  AI Service  │
│   :8000     │    │    :8001     │
└──────┬──────┘    └──────┬───────┘
       │                  │
       │                  ▼
       │          ┌──────────────┐
       │          │    Ollama    │
       │          │   :11434     │
       │          └──────────────┘
       │
       ▼
┌─────────────┬──────────────┐
│  Main DB    │   Mock DB    │
│  (Postgres) │  (Postgres)  │
│   :5432     │    :5433     │
└─────────────┴──────────────┘
```

### Компоненты системы:

1. **Frontend** (React) — пользовательский интерфейс
2. **Nginx** — reverse proxy и статический сервер
3. **Backend API** (FastAPI) — основной API для аутентификации и финансовых данных
4. **AI Service** (FastAPI) — сервис для AI-анализа и работы с конкурентами
5. **Ollama** — локальный AI-сервер для обработки запросов
6. **PostgreSQL** (Main DB) — основная база данных для пользователей
7. **PostgreSQL** (Mock DB) — отдельная БД для финансовых данных

---

## 🔐 Подход к безопасности

### Микросервисная архитектура

Система разделена на независимые сервисы, что обеспечивает:
- **Изоляцию** — сбой одного сервиса не влияет на другие
- **Масштабируемость** — каждый сервис можно масштабировать независимо
- **Безопасность** — четкое разделение прав доступа и ответственности

### Разделение баз данных

Проект использует **две отдельные базы данных**:

1. **Main DB** — хранит пользовательские данные (логины, пароли, профили)
2. **Mock DB** — хранит финансовые данные (счета, транзакции)

**Причины разделения:**
- Финансовые данные будут поступать из отдельного API Альфа-Банка
- Возможность легко отключить финансовую БД без влияния на основную систему
- Изоляция чувствительных финансовых данных
- Упрощение миграции на реальное API банка в будущем

### Локальный AI

Для обеспечения **конфиденциальности данных** используется локальный AI-сервер **Ollama**:
- Все данные обрабатываются локально, без отправки в облачные сервисы
- Полный контроль над конфиденциальной бизнес-информацией
- Независимость от внешних AI-провайдеров
- Соответствие требованиям безопасности финансовых данных

### Дополнительные меры безопасности:

- JWT-токены для аутентификации
- Хеширование паролей
- CORS-политики для защиты от межсайтовых запросов
- Валидация всех входящих данных
- Логирование всех операций

---

## 🎯 Как это работает

### Поток данных:

1. **Пользователь** взаимодействует с Frontend (React)
2. **Nginx** маршрутизирует запросы:
   - `/api/*` → Backend API
   - `/api/ai/*` → AI Service
   - `/ai-api/*` → AI Service
   - `/promotion/*` → AI Service
3. **Backend API** обрабатывает:
   - Аутентификацию пользователей
   - Управление финансовыми данными
   - Бизнес-логику приложения
4. **AI Service** обрабатывает:
   - Классификацию пользовательских запросов (FIN/MRKT)
   - Поиск и анализ конкурентов
   - Генерацию рекомендаций
5. **Ollama** выполняет:
   - NLP-обработку запросов
   - Анализ текстовой информации
   - Генерацию ответов

### Автоматическое заполнение данных

При запуске Backend автоматически выполняет seed-скрипты:
- Создание тестовых пользователей (admin, test)
- Заполнение тестовыми финансовыми данными

Это можно отключить через переменную окружения `AUTO_SEED=false`.

---

## 🔧 Backend

### Технологический стек

- **FastAPI** — современный веб-фреймворк для Python
- **PostgreSQL** — реляционная база данных
- **SQLAlchemy** — ORM для работы с БД
- **Pydantic** — валидация данных
- **JWT** — аутентификация
- **AsyncIO** — асинхронная обработка запросов

### Основные возможности:

- 🔐 Аутентификация и авторизация пользователей
- 💰 Управление финансовыми данными (счета, транзакции)
- 📊 API для аналитики
- 🔄 Автоматическое создание таблиц при запуске
- 📝 Автоматическое заполнение тестовыми данными
- 🛡️ Обработка ошибок и логирование

### API Endpoints:

- `POST /api/auth/login` — вход в систему
- `GET /api/auth/me` — получение информации о текущем пользователе
- `GET /api/amount/` — получение списка счетов
- `GET /api/amount/{id}/transactions` — получение транзакций по счету
- `GET /ping` — проверка работоспособности

### Структура проекта:

```
back/
├── app/
│   ├── api/          # API endpoints
│   ├── core/         # Конфигурация, БД, middleware
│   ├── repo/         # Репозитории для работы с БД
│   ├── schemas/      # Pydantic схемы
│   ├── services/     # Бизнес-логика
│   └── scripts/      # Seed-скрипты
├── solution/         # AI Service
├── main.py           # Точка входа
└── requirements.txt  # Зависимости
```

**📖 Подробная документация:** [back/README.md](back/README.md)

---

## 🎨 Frontend

### Технологический стек

- **React** — библиотека для создания пользовательских интерфейсов
- **Vite** — быстрый сборщик и dev-сервер
- **React Router** — маршрутизация
- **Axios/Fetch** — HTTP-клиент
- **CSS Modules** — стилизация компонентов

### Основные возможности:

- 🏠 Главная страница с обзором финансов
- 📊 Страница аналитики с графиками
- 💳 Управление счетами и транзакциями
- 🔍 Анализ конкурентов
- 💡 AI-рекомендации
- 👤 Профиль пользователя

### Структура проекта:

```
front/
├── src/
│   ├── api/          # API клиенты
│   ├── components/   # React компоненты
│   ├── pages/        # Страницы приложения
│   ├── layouts/      # Макеты страниц
│   └── assets/       # Статические ресурсы
├── public/           # Публичные файлы
└── nginx.conf        # Конфигурация Nginx
```

**📖 Подробная документация:** [front/README.md](front/README.md)

---

## 🤖 AI Service

### Технологический стек

- **FastAPI** — веб-фреймворк
- **Ollama** — локальный AI-сервер
- **DuckDuckGo Search** — поиск информации о конкурентах
- **BeautifulSoup** — парсинг HTML
- **httpx** — асинхронные HTTP-запросы

### Возможности:

- 🧠 Классификация запросов (FIN — финансы, MRKT — маркетинг)
- 🔍 Автоматический поиск конкурентов
- 📄 Парсинг и анализ веб-страниц конкурентов
- 💬 Генерация рекомендаций на основе AI-анализа
- 📊 Стриминг ответов для лучшего UX

### API Endpoints:

- `POST /api/ai/message/` — обработка пользовательских запросов
- `POST /api/competitors/analyze/` — анализ конкурентов
- `GET /health_check/` — проверка работоспособности

**📖 Подробная документация:** [back/solution/README.md](back/solution/README.md)

---

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Main Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=alfahack_db

# Mock Database (для финансовых данных)
MOCK_POSTGRES_USER=postgres
MOCK_POSTGRES_PASSWORD=your_password
MOCK_POSTGRES_DB=alfahack_mock_db

# Backend
SECRET_KEY=your_secret_key_here
ACCESS_TOKEN_EXPIRE_MINUTES=30
PASSWORD_MIN_LENGTH=8
ALGORITHM=HS256

# Auto-seed (автоматическое заполнение данных)
AUTO_SEED=true

# AI Service
OLLAMA_URL=http://ollama:11434/
```

### Порты сервисов

- **Frontend**: 80
- **Backend API**: 8000
- **AI Service**: 8001
- **Ollama**: 11434
- **PostgreSQL (Main)**: 5432
- **PostgreSQL (Mock)**: 5433

---

## 🧪 Тестирование

### Тестовые пользователи

После автоматического seed создаются пользователи:

- **Admin**: `admin` / `admin12345`
- **Test User**: `test` / `test12345`

### Ручной запуск seed-скриптов

Если автоматический seed отключен, можно запустить вручную:

```bash
# Заполнение пользователей
docker-compose --profile seed run --rm seed-users

# Заполнение финансовых данных
docker-compose --profile seed run --rm seed-amounts
```

---

## 📦 Развертывание

### Production

Для production рекомендуется:

1. Использовать переменные окружения из безопасного хранилища
2. Настроить HTTPS через Nginx
3. Использовать production-сборку Frontend
4. Настроить мониторинг и логирование
5. Использовать внешнюю БД вместо Docker volumes
6. Настроить резервное копирование БД

### Docker Compose

Все сервисы настроены для работы через Docker Compose:
- Автоматическое создание сетей
- Health checks для всех сервисов
- Зависимости между сервисами
- Volumes для персистентности данных

---

## 🛠️ Разработка

### Локальный запуск (без Docker)

#### Backend

```bash
cd back
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

#### Frontend

```bash
cd front
npm install
npm run dev
```

#### AI Service

```bash
cd back/solution
uv sync
uv run uvicorn src.main:app --reload --port 8001
```

### Структура проекта

```
AlfaHack/
├── back/              # Backend приложение
│   ├── app/          # Основной код приложения
│   ├── solution/     # AI Service
│   └── tests/        # Тесты
├── front/            # Frontend приложение
├── docker-compose.yaml
└── README.md
```

---

## 📝 Лицензия

Проект разработан для хакатона от Альфа-Банка.

---

## 👥 Команда
Лёха, Денчик, Серей, дизайнер и аналитик