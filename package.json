{
  "name": "alfahack-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd back && uvicorn app.main:app --host 0.0.0.0 --port 8000\" \"cd front && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
